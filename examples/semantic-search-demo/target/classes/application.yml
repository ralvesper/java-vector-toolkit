server:
  port: 8080

vector:
  store:
    provider: in-memory

  # Example for Pinecone:
  # store:
  #   provider: pinecone
  # pinecone:
  #   api-key: ${PINECONE_API_KEY}
  #   host: ${PINECONE_HOST}

  chunking:
    size: 800
    overlap: 100
  search:
    top-k: 5

